# Frontend

Minimal Vite + React frontend for `product`.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

The app attempts to fetch `/api/hello` from the same origin. If your backend runs on a different port (e.g., 3001), either configure a proxy in Vite or enable CORS on the backend while developing.
